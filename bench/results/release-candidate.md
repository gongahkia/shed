# Release candidate bench 2026-06-29

## Environment

- Hardware: Apple M3
- Power: AC connected
- OS: macOS 26.5.1 25F80
- Protocol: no purge; `sudo purge` is unavailable non-interactively in this environment.
- Harness: `.build/release/PicoBench measure`
- Pico app: `Pico.app` generated by `bench/scripts/make_app.sh`

## Result

Ship status: blocked. Do not ship this release candidate.

Reasons:
- Current Pico external-window cold start is `272.661 ms` mean, above the `<150 ms` target.
- Current Zed cold-start re-run is unavailable without closing the user's running Zed session.
- Full competitor matrix is unavailable locally: Sublime Text, VSCode, and CodeEdit are not installed.

## Current Pico run

| App | Runs | Mean startup ms | Min startup ms | Max startup ms | Mean RSS KB |
|---|---:|---:|---:|---:|---:|
| Pico | 20 | 272.661 | 237.908 | 326.350 | 86467 |

## Zed comparison

Current Zed install:
- Zed `1.8.2`
- Bundle version `20260624.160235`

Current Zed re-run attempts:
- Existing Zed process was already running.
- `PicoBench measure --app /Applications/Zed.app --new-instance` failed with `AXObserverCreate failed: -25201`.
- `PicoBench measure --app /Applications/Zed.app --new-instance --args --user-data-dir --args /tmp/pico-zed-bench` failed with `AXObserverCreate failed: -25201`.
- Direct binary launch with temp user data reported `zed is already running`.

Historical committed same-version Zed baseline from `bench/results/baseline-2026-06-28.json`:

| App | Runs | Mean startup ms | Min startup ms | Max startup ms | Mean RSS KB |
|---|---:|---:|---:|---:|---:|
| Zed 1.8.2 | 20 | 470.856 | 330.977 | 679.386 | 182062 |

Against that same-version committed baseline, Pico is `42.1%` faster on mean startup and uses `52.5%` less RSS at first-window measurement. Current Zed cold-start superiority cannot be verified without closing Zed first.

## Notes

- [Inference] The current mean startup miss is still dominated by the app-bundle/window launch path rather than `--bench-exit-on-ready`, matching `bench/notes/coldstart-audit.md`.
- Raw Pico runs were collected as 20 JSON lines from `PicoBench measure --app Pico.app`; only the aggregate is committed.
