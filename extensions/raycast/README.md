# Olly Raycast Extension

Stub Raycast extension exposing the top 10 `ollyctl` IPC commands.

## Develop

```sh
cd extensions/raycast
npm install
npm run dev
```

Set `ollyctlPath` in Raycast preferences if `ollyctl` is not on `PATH`.

Store submission is deferred until olly v0.1.
