{
  "schemaVersion": 1,
  "suite": "alpha-promotion-v1",
  "gates": [
    {"id":"preflight","required":true,"remediation":"Run scripts/private_alpha_preflight.sh and satisfy every required blocked row."},
    {"id":"build","required":true,"remediation":"Resolve release build failures with swift build -c release."},
    {"id":"tests","required":true,"remediation":"Resolve the failing swift test log."},
    {"id":"lsp-matrix","required":true,"remediation":"Install/configure the missing language server and rerun scripts/lsp_matrix.sh."},
    {"id":"private-alpha","required":true,"remediation":"Use the scenario log from scripts/private_alpha_acceptance.sh to fix or provision the blocker."},
    {"id":"self-hosting","required":true,"remediation":"Use scripts/self_hosting_acceptance.sh repro.sh and screenshot artifacts."},
    {"id":"failure-injection","required":true,"remediation":"Use scripts/failure_injection.sh scenario logs and repro.sh."},
    {"id":"release-ui","required":true,"remediation":"Grant Accessibility/Screen Recording if blocked; otherwise use release UI screenshot/log artifacts."},
    {"id":"performance","required":true,"remediation":"Inspect bench/scripts/regression.sh output and its linked latency/memory artifacts."}
  ]
}
