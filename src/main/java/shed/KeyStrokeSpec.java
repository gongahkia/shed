package shed;

final class KeyStrokeSpec {
    final int keyCode;
    final char keyChar;

    KeyStrokeSpec(int keyCode, char keyChar) {
        this.keyCode = keyCode;
        this.keyChar = keyChar;
    }
}
